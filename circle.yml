machine:
  hosts:
    circlehost: 127.0.0.1
    dev.mycompany.com: 127.0.0.1
database:
  override:
    - cp config/database.yml.tmp config/database.yml
test:
  override:
    - nohup bash -c "./test-builder.sh &"